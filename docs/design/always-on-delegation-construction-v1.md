**Created:** 2026-09-07 · **Last-updated:** 2026-09-07 · **Status:** FINDING

# A bounded second agent must justify its extra work

Chapter14 constructs the existing Inquiry, quote, immutable delegation, expiry,
shared call reservation and read-only research loop. No runtime change. The full
overhaul remains active; fourteen drafts exist and two remain.

The checkpoint independently verifies five authored arithmetic boundaries before
comparing the worker with the function. Agreement between two calls to quote is
not treated as an independent proof of the business rule. The fixed calculation
retains the direct function as its architecture recommendation.

Two actual process cases pause research inside its model call while the parent
process completes actual shop work. One child is released to produce five tubs
for41 guests at2500pence, with2 calls14 estimated pence. Repeated research work is
IDLE, not an exactly-once external-delivery claim. The second child is cancelled
after its stock parent is DONE. It returns AUTHORITY_STOP, records no tool
observations and becomes CANCELLED without rewriting the parent's completed
result. Unclaimed work expires through actual elapsed time. Replacement after
actual lease expiry cannot reset an exhausted one-call contract.

All scenario model usage is billed to Lucy:10 calls26 estimated pence. Extra
assistant_daily rows belong to intake accounting for research sessions and have
zero model usage. The prototype's original one-row assertion was an incorrect
instrument; v3 verifies the billed columns. Inventory and reservations remain
unchanged and there are zero supplier orders.

Existing live delegation evidence retains an invalid no-argument tool-schema
run and the repaired exact-Inquiry run with local qwen3. Its recorded repaired
source hash matches the current delegation.py bytes. The new process fixture is
deterministic and does not claim a new live-model reliability measurement or a
performance benefit from its deliberately paused model call.

Remaining work: deployment and integrated business-day chapters, latest-release
Linux installation, readable reconciled report, publisher proposal, exact-source
site integration and human/phone acceptance. Live Telegram still awaits the
operator's ready response. Draft-shape scores are not publication acceptance.
