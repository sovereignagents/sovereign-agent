**Created:** 2026-09-07 · **Last-updated:** 2026-09-07 · **Status:** FINDING

# A replacement must exclude an old holder that is still alive

Chapter 10 constructs the existing claim, lease, generation and current-holder
checks. No runtime change was needed for the new failure experiment. The full
overhaul remains active; ten chapters are drafted and six remain planned.

The checkpoint runs two fresh POSIX process cases with a separate HTTP supplier
for each. An old worker claims an assignment for two seconds and records an
operator-approved six-tub order. One case actually sends SIGKILL and verifies
the signal exit. The other leaves the old process alive. After observed real
lease expiry, the actual store worker acquires generation two and completes the
existing approved operation. A model that raises on invocation proves that no
fresh reasoning is used to reconstruct this order.

The live old process is then released and attempts transcript observation,
completion and supplier execution with its stale claim. All three are refused.
Each independent supplier database contains exactly the original operation,
with one purchase and 1500 spent pence. The checkpoint is separate from the
existing actual agent-serve SIGKILL-after-remote-commit test; that older test
explicitly accelerates its lease and backoff fixture, which the chapter states.

The manuscript distinguishes process liveness from current authority, and
ordinary process generation from the restore epoch. Its guarantee is about
controlled writes on one host with SQLite, not arbitrary host code, network
filesystems or distributed clock coordination. It does not claim that replacing
a worker can recall a previously admitted supplier request.

The pinned OpenClaw reply-delivery source compares current session identity and
optional lifecycle and writer-run identity for authority-bearing payloads.
Missing authority has its own permitted path. This directly informs the book's
comparison and contradicts an unqualified uniqueness claim from the supplied
feedback. We inspected source and did not run OpenClaw or establish universal
provider coverage. The source hash and narrow scope are retained in the
comparison receipt.

The actual isolated Linux host now runs approval release a89ce6a with schema25.
The guarded switch retained orders, inventory and spending and completed fresh
work. Attempting to switch back to schema24 release40bef87 was refused before
stopping services. The raw switch receipt's schema field describes the prior
schema; the independent post-switch query records schema25. The evidence
wrapper makes that distinction explicit.
