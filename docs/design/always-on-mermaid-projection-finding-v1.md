**Created:** 2026-09-07 · **Last-updated:** 2026-09-07 · **Status:** FINDING

# The rendered parser is a separate construction check

The first exact-source site projection parsed all52 diagrams with the actual
Mermaid package. Chapter6 diagram3 failed at a semicolon inside a sequence note.
Python checkpoint and shape scores did not parse Mermaid and therefore did not
establish this property. The source note now uses an ordinary conjunction; its
process-exit meaning is unchanged. The site transform never repairs source prose.

The same projection exposed a distinct bridge limitation: v1 selected-listing
wrapping covered only Python and Bash, leaving the Chapter5 TOML skill listing
unbound. The v2 transform includes TOML and a direct regression fixture. All52
figures and97selectedlistings are retained; counts were not reduced to pass.

Remaining: run the actual Mermaid parser on the corrected source, re-pin the
site and verify its full gate/render. This finding does not grant publication
acceptance. Existing receipt versions remain evidence of their original source.
