**Created:** 2026-09-07 · **Last-updated:** 2026-09-07 · **Status:** FINDING

# An installed service must preserve business history

Chapter15 constructs the existing service unit, ownership-aware installer, health,
SQLite backup and paused same-inode restore. No runtime change. Portable checkpoint
uses an actual independent supplier process, rejects repeated backup destination
and changed recovery digest, preserves the snapshot hash and active inode, refuses
an old open connection and supplier epoch, imports two retained receipts after a
snapshot with one local order, accounts2600pence once, preserves8 vanilla tubs on
hand and4 strawberry pending, and completes fresh work. Lost model usage remains
marked incomplete. The checkpoint explicitly does not run systemd.

The isolated Linux host now runs frozen release6185673 for both user services,
upgraded froma89ce6a at schema25. A unique main-service request completed and the
two orders, inventory and spending remained unchanged. linux-ch15-release-v1.json
records the switch; prior pinned receipts cover actual reboot, worker termination,
backup/recovery and reviewed compatible code switches. No new month-long uptime,
phone or production-provider claim follows from these finite observations.

The manuscript distinguishes SQLite-only snapshots from full state/configuration
retention, compatible code rollback from old-state restore, process liveness from
business progress, and fresh model grants from reconstructed historical usage.
Draft-shape score100 follows discoverable exercise, expected-observation, summary
and vocabulary headings plus an actual chapter10 cross-link. Earlier temporary
score76/93 receipts remain available; no gate was weakened. Human/rendered review
and exact-source publication integration remain open.

Full overhaul remains active. Chapter16, integrated report, proposal, site migration
and live phone acceptance remain required. Source budgets are unchanged.
