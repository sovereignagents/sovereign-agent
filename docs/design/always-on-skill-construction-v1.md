**Created:** 2026-09-07 · **Last-updated:** 2026-09-07 · **Status:** FINDING

# Local skill construction and bounded reads

Chapter 5 extracts the inherited opening procedure into the existing local TOML format. It constructs the strict model, bounded file read, immutable staging, active-configuration snapshot and evaluated activation; exercises changed-content, missing-case and stale-baseline refusals; and passes selected skill guidance plus retained preferences into the actual owned loop. Requirements filter eligibility but do not grant dispatcher authority. Thirteen Python examples and thirteen output pairs execute cumulatively. The three visible evaluation cases are opening, exact threshold and reserved stock. The larger evaluation framework is developed in Chapter 12; held-out cases are not used to author this chapter.

A deterministic growth reproduction against cec0452 made the old stage_skill read 2,000,000 bytes before rejecting an originally small file. The final read_skill opens once, observes the descriptor's regular-file type and size, and reads at most 16,385 bytes. POSIX no-follow and nonblocking flags refuse a final symlink and a FIFO wait on the taught macOS/Linux hosts. Four tests cover growth, replacement after open, exact byte bounds, directories/symlinks and a subprocess-bounded FIFO refusal. This is not a filesystem snapshot, parent-directory sandbox or portability claim for missing POSIX flags. In-place concurrent writes can alter returned bytes; validation and the stored digest apply to exactly those bounded returned bytes.

Staging remains semantic-content immutable per name/version. Formatting-only TOML changes preserve validated content and the first raw source digest. A staged candidate is inactive. Activation requires a nonempty required-case set, literal passing booleans, an unmutated candidate and unchanged active configuration. Evaluation cannot bless its own truth: the chapter labels fixed-boolean boundary probes separately from the actual scenario evaluator, and authored model responses separately from live observations.

The live checkpoint passed three visible cases and the final cumulative shop turn with qwen3:latest, reasoning none, model digest 500a1f067a9f782620b40bee6f7b0c89e17ae61f686b92c24933e4ca4b2b8b41. ch05-live-construction-v1.json retains the contexts, tool observations, final answers, call counts and timing. Unlike Chapter 4's checkpoint, it does not prepend the old Python procedure: the active skill is the source of guidance. Six vanilla and four strawberry drafts total 2600 pence; purchases remain zero. This single construction run is not a reliability estimate or publication acceptance.

The Hermes comparison is limited to pinned directory packaging and progressive loading at d538f4e9297d7fa46193f638215d002d7a22edd7 tools/skills_tool.py. Trade-off interpretation is labeled, with an experiment that could justify richer referenced content. No marketplace, incident-count or comparative security claim is made.

The full overhaul remains active. Six chapters are drafted; ten remain. Live phone credentials, current-schema Linux maintenance/account recovery, integrated day, publisher proposal, exact-source site migration and rendered review remain outstanding.
