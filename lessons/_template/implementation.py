"""Lesson implementation (template).

This file re-exports the new class or function you added to the main
sovereign_agent package. Keeping the lesson's code alongside the rest of
the package (not in a separate lessons-only location) is deliberate: it
means refactors to the spine that rename or relocate something will
break this import, and the refactoring PR has to either update the
lesson or mark it deprecated. Drift is caught by the type system.

Replace the placeholder below with your actual re-export:

    from sovereign_agent.<path>.<module> import <YourNewClass>  # noqa: F401
"""
